using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class DreamLandManager : MonoBehaviour
{
    private Dictionary<string, Renderer> tiles_materials = new Dictionary<string, Renderer>();
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
